<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.adarshjayakumar.mediaplayer.MainActivity">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Play"
        android:id="@+id/playSong"
        android:layout_below="@+id/pauseSong"
        android:layout_alignStart="@+id/pauseSong"
        android:layout_marginTop="23dp"
        android:onClick="songPlay" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Pause"
        android:id="@+id/pauseSong"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="83dp"
        android:onClick="songPause"
        android:nestedScrollingEnabled="false" />

    <SeekBar
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/volumeBar"
        android:layout_below="@+id/playSong"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="46dp"
        android:indeterminate="false" />

    <SeekBar
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/playBar"
        android:layout_below="@+id/volumeBar"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="53dp"
        android:indeterminate="false" />
</RelativeLayout>
